/**
 * 前端静态数据的口径说明。
 *
 * 这里曾经还放着首页两条链路的步骤（DOCUMENT_CHAIN / QUERY_CHAIN）。
 * 平台总览改版后，首页的三条链路直接读 architecture-data 里已经定义好的
 * workflow.pipeline ——那份数据同时供「技术栈与架构」页和导出图使用，
 * 再在首页另写一份步骤，改了这头忘了那头只是时间问题。
 *
 * 这个文件里**不许**出现任何看起来像实时指标的数字（访问量、成功率、成本）。
 */

/** 凡是展示前端静态配置的位置都要带上这句话。 */
export const MOCK_NOTICE = "静态配置数据 · 非实时运行数据";
